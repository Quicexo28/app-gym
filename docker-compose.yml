services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: coach_ai
      POSTGRES_USER: coach
      POSTGRES_PASSWORD: coach
    ports:
      - "5432:5432"
    volumes:
      - coach_ai_pg:/var/lib/postgresql/data
volumes:
  coach_ai_pg:
