# training_core (Phase 1)

This module will host:
- data schema (ORM models)
- session validation
- individual normalization
- base derived metrics

Principle: **data -> normalization -> derived metrics**, with unit-tested rules.
